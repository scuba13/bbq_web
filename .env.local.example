# Copie este arquivo para .env.local e preencha sua chave
# .env.local nunca vai para o git

# Chave do Google AI Studio (https://aistudio.google.com/app/apikey)
# Usada em desenvolvimento com o mock server.
# Em produção a chave vem do ESP32 via /api/v1/ai/config
REACT_APP_GOOGLE_AI_KEY=sua_chave_aqui
